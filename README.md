
# PRAVA — Option A (Production-Ready, Fully Populated)

This repository contains:
- Complete Platinum architecture
- Runnable backend (Fastify)
- Docker + configs
- Stub files in every module (no empty dirs)

Run:
  docker compose up --build
