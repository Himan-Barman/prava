# Notification Flow

Implemented:

- Notification list with actor profile projection.
- Unread count.
- Mark one notification as read.
- Mark all notifications as read.
- Notification preferences through user settings.
- Durable notification rows for follows, likes, comments, mentions and chats.

The outbox layer is ready for provider-specific push and email delivery workers.
